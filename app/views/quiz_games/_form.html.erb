<%= form_with(model: quiz_game) do |form| %>
  <% if quiz_game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quiz_game.errors.count, "error") %> prohibited this quiz_game from being saved:</h2>

      <ul>
        <% quiz_game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
